import styled from "styled-components";
import Select from "react-select";

export const CustomSelect = styled(Select)<any>`
  & .switchlanguagestyled__customselect-fvsyrn: {
    display: none;
  }
`;
